# The Diniz Studio

Website for thedinizstudio.com.

Next.js, Tailwind CSS, GSAP and Lenis.

```bash
pnpm install
pnpm dev
```

- `components/sections/Hero.tsx` scroll-driven logo intro (frames in `public/hero`)
- `content/work.ts` selected projects
- `styles/globals.css` colour and type tokens
